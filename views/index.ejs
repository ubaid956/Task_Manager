<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Home</title>
  <link rel="stylesheet" href="/stylesheets/style.css"/>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-zinc-900 text-white min-h-screen flex items-center justify-center">
  <div class="w-full max-w-2xl p-8 bg-zinc-800 rounded-xl shadow-md">
    <nav class="mb-6">
      <a class="text-blue-400 hover:underline" href="/tasks">View All Tasks</a>
    </nav>
    <h1 class="text-3xl font-bold mb-6">Task Manager</h1>

    <form action="/create" method="post" class="flex flex-col gap-4">
      <input
        class="px-4 py-3 rounded-md bg-zinc-100 text-black placeholder-zinc-600 focus:outline-none focus:ring-2 focus:ring-blue-500"
        type="text"
        name="title"
        placeholder="Task Title"
        required
      />
      <textarea
        class="px-4 py-3 h-32 rounded-md bg-zinc-100 text-black placeholder-zinc-600 resize-none focus:outline-none focus:ring-2 focus:ring-blue-500"
        name="description"
        placeholder="Description..."
        required
      ></textarea>
      <button
        class="bg-blue-600 hover:bg-blue-700 transition px-6 py-3 rounded-md text-white font-semibold"
        type="submit"
      >
        ➕ Create Task
      </button>
    </form>
  </div>
</body>
</html>
